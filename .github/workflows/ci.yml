name: CI

on:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup mise
        uses: jdx/mise-action@v2
      - name: Build
        run: mise run build
