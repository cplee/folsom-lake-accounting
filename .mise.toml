[tools]
hugo = "0.104.1"
"npm:vercel" = "latest"

[tasks.dev]
description = "Run Hugo development server with drafts"
run = "hugo server -D"

[tasks.build]
description = "Build Hugo site with minification"
run = "hugo --minify"
